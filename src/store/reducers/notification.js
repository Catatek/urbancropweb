// import { Map, fromJS } from 'immutable';
// import { FETCH_NOTIFICATIONS } from '../types/notification';

// export default (state = Map(), { type, ...action }) => {
//   switch (type) {
//     case FETCH_NOTIFICATIONS.SUCCESS:
//       state = state.set('notifications', fromJS(action.notifications));
//       return state;
//     default:
//       return state;
//   }
// };
